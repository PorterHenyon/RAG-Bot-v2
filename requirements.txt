# It's recommended to install libraries in a virtual environment
# python -m venv venv
# source venv/bin/activate (or .\venv\Scripts\activate on Windows)
# pip install -r requirements.txt

python-dotenv
discord.py
google-generativeai
